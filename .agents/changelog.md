# りんごちゃん薬局 シフト作成ツール 作業履歴 (Changelog)

このファイルは、**AIセッションの作業履歴** と **シフト生成ロジックの変更ログ** を一元管理する **長期記憶用ファイル** です。
普段のセッションでは読み込む必要はありません。過去の経緯を調査する必要がある場合のみ参照してください。

エントリは **フェーズ別章立て + 章内は新しい順** で並んでいます。

---

## 章立て索引

- [Phase 0: AIエージェント環境構築（2026-05）](#phase-0-aiエージェント環境構築2026-05)
- [Phase 1: 要件定義（2026-05〜）](#phase-1-要件定義2026-05)
- [Phase 2: DB構築・初期データ（未着手）](#phase-2-db構築初期データ未着手)
- [Phase 3: 画面実装（未着手）](#phase-3-画面実装未着手)
- [過去ログ: シフト生成ロジック変更履歴（旧プロジェクト踏襲分）](#過去ログ-シフト生成ロジック変更履歴旧プロジェクト踏襲分)

---

## 運用ルール

### handover.md からの退避タイミング

`.agents/handover.md` の「Last Action」が **5件を超える** または **2週間以上前のエントリが残っている** 場合、本ファイルの該当章末に退避させる。

### 退避時のフォーマット

```markdown
### [YYYY-MM-DD] エントリタイトル

- 変更内容の要約
- 検証結果（テスト件数・lint等）
- 関連コミット / PR / ドキュメント
```

### シフト生成ロジック変更の記録

シフト生成アルゴリズム（`js/generate.js`）の変更は、本ファイルの「シフト生成ロジック変更履歴」章に追記する。
`docs/changelog.md` は廃止済み（このファイルにポインタのみ残す）。

---

## Phase 0: AIエージェント環境構築（2026-05）

### [2026-05-05] .agents/ ディレクトリ初期化

- ボイラープレート（`ai-agent-boilerplate`）最新版を適用し、AI挙動ファイル群を新規作成：
  - `CLAUDE.md`（プロジェクトルート）— りんごちゃん薬局固有情報で完全カスタマイズ
  - `.agents/handover.md` / `.agents/lessons.md` / `.agents/changelog.md`（本ファイル）
  - `.agents/lessons/encoding.md` / `general.md` / `README.md`
  - `.agents/workflows/`（7ファイル: add-page / codebase-guide / css-conventions / deploy / development-workflow / security-checklist / terminal-encoding）
  - `.agents/skills/frontend-skill.md`
- `docs/changelog.md` の既存ログを本ファイルに統合。`docs/changelog.md` はポインタのみ残す。

---

## Phase 1: 要件定義（2026-05〜）

### [2026-05-06] シフト生成条件の最終整理・README削除

- `docs/ringo-requirements.md` に基づき、自動割り当て対象（薬剤師4名＋事務3名）の勤務条件・固定休・シフト枠を整理して確認。
- 古い仕様（11名体制など）が残っていたルートディレクトリの `README.md` を削除。
- `docs/shift-generation-requirements.md` およびアルゴリズムについて、割り振り優先度（常勤優先 → 湯本 → 鈴木 → 事務）と30パターン評価による全体バランスの考慮が実装されていることを検証・確認。

*(着手中。完了エントリは随時追記)*

---

## Phase 2: DB構築・初期データ（未着手）

*(Supabase新規プロジェクト作成後に追記)*

---

## Phase 3: 画面実装（未着手）

*(M3以降の着手後に追記)*

---

## 過去ログ: シフト生成ロジック変更履歴（旧プロジェクト踏襲分）

> 以下は旧プロジェクト（大黄薬局等）からコピーされたシフト生成ロジック（`generate.js`）の変更履歴。
> りんごちゃん薬局向けにアルゴリズムを改修した際は、本章の上部（新しい順）に追記すること。

---

### [2026-03-25] 配置均等化・Undo/Redo・集計列

#### 1. 事務パートの配置均等化（debtベース）
- **旧**: 累積ペース + 優先度ベース → メインスタッフが前半に集中
- **新**: 負債（debt）ベースのスコアリングに変更
  - `debt = (target × progress) - actual` で「今この時点で何日遅れているか」を算出
  - debtが大きいスタッフが優先的に出勤 → 月全体で自然にインターリーブ
  - priorityは同debtスタッフ間のタイブレーカーに格下げ

#### 2. ペース配分閾値の調整
- `ahead > 0.15` → `ahead > 0.08` に変更（前半固め打ち防止を強化）

#### 3. 連勤上限の統一
- `max_consecutive_days: 4` をDB（work_conditions）に追加

#### 4. Undo/Redo/Reset機能
- 手動セル変更の履歴管理（最大50件）
- Reset: シフト生成直後の状態に戻す
- `generate.html` にボタン追加済み

#### 5. ガントチャート集計列
- スタッフ名の右横に出勤数/公休数を表示
- スタッフ別の閾値で色分け（白/黄/赤）

#### 6. 条件チェック文言の簡潔化
- 各チェック項目の`text`を短く読みやすく修正

#### 7. 生成パラメータ改善
- 試行回数: 8 → 30

#### 8. シフト再生成・リセット時の手動変更フラグ（赤バッジ）クリア修正
- **課題**: シフト再生成やリセット後も、手動変更を示す赤バッジが残り続けるバグが存在した。
- **原因**: 以前の「悪化防止ロジック」によって、以前の手動変更データが維持されていたため。
- **対応**: 悪化防止（既存維持）ロジックを撤去。シフト再生成時は常に新規の生成結果を採用し、手動変更フラグが確実にクリアされるよう修正。またリセットボタン押下時にも明示的にフラグをクリアするよう安全策を追加。

---

### [2026-03-25] 条件チェックロジックのリファクタリング

#### 背景
`scoreShifts()`（スコアリング）と`renderConditionsCheck()`（UIパネル表示）で同一のチェックロジックが約400行にわたり二重実装されていた。片方を修正してもう片方を忘れるとバグが発生する温床になっていた。

#### 変更内容

##### 1. `runAllChecks()` 共通関数の導入
- 全チェック項目を実行し、構造化データ（`globalItems`, `staffChecks`, `bonusItems`）を返す
- `scoreShifts()` と `renderConditionsCheck()` の両方がこの結果を利用

##### 2. ヘルパー関数のモジュールレベル昇格
以下の関数を `renderConditionsCheck` 内部のクロージャからモジュールレベルに移動：
- `_staffAssignments`, `_workDays`, `_restDays`, `_countPattern`
- `_maxConsecutiveWork`, `_maxConsecutiveWorkIncludingDispense`
- `_countSundays`, `_checkConsecutiveRestPairs`
- `_restOverlap`, `_countCrossStore`, `_getWeeklyBreakdown`, `_dispenseCount`

##### 3. コード量の変化
- **Before**: 2320行 / **After**: 1993行（**-327行**）

---

### [2026-03-24] シフト生成ロジックの改善

#### 週またぎ連勤制限
- `canWork()`に`alternating_weeks`に基づく連勤制限を追加

#### 6連勤防止
- `computeRestDays()`に`enforceGaps`ロジックを追加
- 公休間に6日以上の空白が生じないよう強制的に休日を挿入

#### 調剤日数考慮
- 調剤（dispense）日を月間出勤カウントの初期値に加算

#### 条件チェックパネルの改善
- チェック項目の説明文を詳細化
- 優先順位順にソート（絶対 > 高 > 中 > 低）

#### 配置アルゴリズムの修正
- `tryAssignOffice()`のスコア計算で、店舗優先順位をペーシングより重視するよう変更
